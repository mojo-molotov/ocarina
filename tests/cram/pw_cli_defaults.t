Unset flags fall back to declared defaults (Playwright launcher)

  $ "$PYTHON" "$TESTDIR/_demo_pw_cli.py" --browser chromium
  browser=chromium
  profile_path=None
  headless=True
  workers=5
  wait_timeout=10
  logger=terminal+file
  only=()
  exclude=()
